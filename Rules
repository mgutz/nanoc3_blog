#!/usr/bin/env ruby

require 'pp'

preprocess do
  create_tag_pages
end

compile '/assets/css/*/' do
  filter :sass
end

route '/assets/css/*/' do
  item.identifier.chop + '.css'
end

compile '*' do
  filter :haml
  layout 'default'
end

route '*' do
  route_path(item)
end

layout '*', :haml
